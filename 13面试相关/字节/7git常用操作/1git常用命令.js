/**
 * git 初始化： git init ,clone: git clone url
 * 
 * git 配置： git config --list
 * 
 * 
 * git add: git add [f1] [f2]  // git add [dir] // 
 * 
 * git 分支： git branch git branch -r git branch -a  // 新建分支： git branch [branch-name] ,git checkout -b [branch-name]
 * 
 * # 建立追踪关系，在现有分支与指定的远程分支之间
$   git branch --set-upstream [branch] [remote-branch]
 * 
合并指定的分支到当前分支： git merge [branch-name]

删除分支： git branch -d [branch-name]

删除远程分支：git push origin --delete [branch-name]
 * 
 * 
 */