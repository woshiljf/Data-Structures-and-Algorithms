/**
 * vuex状态管理包括三个流程
 * state，驱动应用的数据源；
   view，以声明方式将 state 映射到视图；
   actions，响应在 view 上的用户输入导致的状态变化。
 * 
 * 包括四个部分：state，mutations同步的修改数据,actions，异步请求，提交到mutaions，修改数据
 * getters，响应数据，对数据进行拦截处理,modules
 * 
 * 
 */